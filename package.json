{
  "name": "cinema-tickets-javascript",
  "description": "This service exposes several endpoints to allow booking cinema tickets in some imagined deployment",
  "version": "1.0.0",
  "engines": {
    "node": ">=16.15.1"
  },
  "type": "module",
  "scripts": {
    "start": "node main.js",
    "dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "jest": "28.1.1",
    "mocha": "10.0.0",
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
