openapi: '3.0.2'
info:
  title: Ticket Booking Service
  version: '1.0'
servers:
  - url: https://localhost:3000/
paths:
  /v1/tickets/purchase:
    $ref: "./swagger/tickets/purchase.yaml"
components:
  schemas:
    $ref: "./swagger/components/order.yaml"
